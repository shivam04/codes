Var mp = dict();
Def f1( a , sum) :
	
For i in range(0,len(a)):
Temp = sum-a[i]
if(mp[temp]!=-1):
	Return (mp[temp],i)
	mp[a[i]]=i

	
Int isPalin(struct Node*head)
{
//Int l = calc(head);
// initialisation
while(head!= null and head->next != null)
{
	Fp = fp->next->next;
		
           Prev_sp  = sp;
           Sp =  sp->next; 
}
if(fp != NULL)
{
	Midn = sp;
            Sp = sp->next;

}

Half2 = sp;
prev_sp ->next=NULL;
reverse(&half2);

Tmp 1 = head;
Tmp = half2;
wh



}
